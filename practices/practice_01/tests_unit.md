# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| SEC-1 | Маскирование секретов перед вызовом LLM | `diff` содержит `token=TEST_SECRET` | В сформированном промпте вместо секретов — `[REDACTED]`; LLM получает замаскированные значения | review_service.py:15 — сейчас сырой diff включается в промпт |
| OUT-1 | Формирование структуры ответа | Ответ сервиса | Структура содержит `summary`, `risks` (≤ 3), `checks`; без лишних полей | review_service.py:17 → api.py:9 — сейчас `{ "comment": ... }` |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02.
- Что проверили и исправили сами: ограничили unit до проверок, напрямую вытекающих из diff и правил; исключили нерелевантные проверки snake_case и i18n.
