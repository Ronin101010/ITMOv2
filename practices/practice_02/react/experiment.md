# ReAct

- Цель: уточнить сценарии `product_management.md` и добавить boundary-кейс `len(diff) == 20000` с проверяемыми AC.
- Доступные входы: `product_management.md`, Context Pack (`API-1`, `OUT-1`).
- Разрешённые действия: читать файлы, предлагать конкретные изменения текста, формулировать AC; задавать один уточняющий вопрос при двусмысленности.
- Запрещённые действия: менять код, добавлять правила вне Context Pack, тестировать OBS-1 в E2E.
- Максимальное число шагов: 3.
- Условие остановки и вопроса человеку: если трактовка `API-1` по границе 20000 остаётся двусмысленной — спросить, считается ли ровно 20000 валидным.

Вопрос человеку: подтверждаете, что `len(diff) == 20000` относится к валидному диапазону и не попадает под 413? Ответ: да.

## Запрос

Пройди по сценарию ReAct для `product_management.md`. Шаг 1: оцени текущие AC и найди пробелы по `API-1` и `OUT-1`. Шаг 2: предложи точные правки текста: добавить сценарий `len(diff) == 20000` и уточнить структуру ответа. Шаг 3: проверь согласованность с `tests_e2e.md`.

## Наблюдаемые действия и результаты

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | Прочитал AC и Context Pack | Нет явного кейса `== 20000`; формат ответа описан, но без запрета лишних полей | Продолжить: предложить правки |
| 2 | Предложил добавить сценарий `== 20000` и уточнить OUT-1 | Текст правок согласуется с Context Pack | Продолжить: сверить с E2E |
| 3 | Сопоставил с `tests_e2e.md` | E2E обновлён аналогично; расхождений нет | Остановиться |

## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_01/product_management.md` — «User stories и acceptance criteria»
- Изменение: добавлен сценарий `len(diff) == 20000` (валидно) и уточнение, что ответ содержит только `summary/risks/checks`
- Как проверили: сопоставили с Context Pack и обновлённым `tests_e2e.md`
- Что отклонили: проверку OBS-1 на уровне E2E — непрактично и не требуется
